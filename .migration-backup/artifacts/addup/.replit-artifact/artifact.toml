kind = "web"
previewPath = "/"
title = "Addup"
version = "1.0.0"
id = "artifacts/addup"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25870

[services.development]
run = "pnpm --filter @workspace/addup run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/addup", "run", "build" ]
publicDir = "artifacts/addup/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25870"
BASE_PATH = "/"
